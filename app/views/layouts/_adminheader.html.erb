<nav class="navbar navbar-expand-lg px-lg-5 bg-info fixed-top">
  <div class="container-fluid">
    <div style="">
      <%= link_to 'System Management', admin_root_path, class: 'navbar-brand text-dark' %>
    </div>
    <div class="btn-group">
      <div class="dropdown mx-3">
        <a href="#" class="d-flex align-items-center text-black text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
          <span class="d-none d-sm-inline mx-1">Change Language</span>
        </a>
      </div>
      <div class="dropdown mx-2">
        <a href="#" class="d-flex align-items-center text-black text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
          <span class="d-none d-sm-inline mx-1">Username</span>
        </a>
        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark text-small shadow">
          <% if admin_user_signed_in? %>
            <%= button_to "Profile", admin_root_path, class: 'dropdown-item' %>
            <%= button_to "Log Out", destroy_user_session_path, { onclick: "return confirm('Are you sure you want to delete?')", method: :delete, class: "dropdown-item" } %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "dropdown-item" %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>