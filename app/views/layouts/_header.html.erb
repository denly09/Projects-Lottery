<nav class="navbar navbar-light bg-info sticky-top">
  <div class="container">
    <a class="navbar-brand"><i class="bi bi-cash-coin"></i></a>
    <div class="d-flex p-1">
        <%=  button_to current_user.coins, "", class:'btn btn-outline-success bi bi-coin' if current_user %>
      <button class="btn btn-outline-success mx-1" type="submit">Claim</button>
      <button class="btn btn-outline-success mx-1" type="submit">Language</button>
      <% if user_signed_in? %>
        <%= button_to "Log out", destroy_user_session_path, { onclick: "return confirm('Are you sure you want to log_out?')", method: :delete, class: "btn btn-outline-danger mx-1" } %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary mx-1" %>
      <% end %>
      <div class="btn-group">
        <button type="button" class="btn btn-warning dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" ></button>
        <ul class="dropdown-menu dropdown-menu-end">
          <li>
            <button class="dropdown-item" type="button"><%= link_to "Profile", users_mes_path, class: 'btn btn-outline-primary' %></button>
          </li>
<!--          <button class="dropdown-item" type="button "></button>-->
          <li>
          </li>
        </ul>
      </div>

    </div>
  </div>
</nav>
